package main

func submitPotaReport(inFile []string, contacts []Contact, config *Config) error {
	/*
		m := gomail.NewMessage()
		m.SetHeader("From", "denisovenator@gmail.com")
		m.SetHeader("To", "denisovenator@gmail.com")
		m.SetHeader("Subject", "Hello!")
		m.SetBody("text/html", "Hello <b>Bob</b> and <i>Cora</i>!")
		m.Attach("input.hl", gomail.Rename("my.hl"))

		d := gomail.NewDialer("smtp.gmail.com", 587, "denisovenator@gmail.com", "ulyexjibnwkhvbhw")

		if err := d.DialAndSend(m); err != nil {
			panic(err)
		}
	*/
	return nil
}
